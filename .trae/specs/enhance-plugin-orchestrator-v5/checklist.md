# v5 验证清单

- [ ] PluginOrchestrator 单例存在，`Instance` 静态属性返回唯一实例
- [ ] `PluginOrchestrator.InitializeAsync()` 调用一次后，`_manager.GetLoadedPlugins()` 非空
- [ ] `PluginOrchestrator.ScanTargetAsync("192.168.3.92", [80, 554])` 返回 List<VulnerabilityResult>
- [ ] 重复调用 `ScanTargetAsync` 不重新 LoadAllPlugins
- [ ] `ScanTargetAsync` 指定 `selectedPluginIds` 时仅运行指定插件
- [ ] `TryRunAsync(marketPlugin)` 不调用网络（即使参数 ip=真实 IP 也不扫描）
- [ ] PluginQuickInvokeWindow 打开后默认全选所有插件
- [ ] 输入 `80,443,554` 解析为 [80, 443, 554]
- [ ] 输入 `1-1024` 解析为 [1..1024]
- [ ] 主窗口顶部"🔌 插件扫描"按钮可见可点
- [ ] 点击"🔌 插件扫描"弹出 PluginQuickInvokeWindow
- [ ] 综合扫描结果 DataGrid 右键菜单含"🔌 用插件深挖"
- [ ] 摄像头扫描结果 DataGrid 右键菜单含"🔌 用插件深挖"
- [ ] 扫描历史 DataGrid 右键菜单含"🔌 用插件深挖"
- [ ] 插件商店窗口操作列含"▶ 试运行"按钮
- [ ] 点击"▶ 试运行"显示模拟结果（不调用网络）
- [ ] 插件日志窗口顶部含"▶ 重新执行选中"按钮
- [ ] 综合扫描 `:1510` 改用 `PluginOrchestrator.Instance.ScanTargetAsync`
- [ ] 综合扫描后 `data/plugin_executions/{date}/*.json` 仍正常生成
- [ ] 编译 0 错误 0 警告

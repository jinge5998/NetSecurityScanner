# NetSecurityScanner v1.0.1.5 发布说明

> 📅 发布日期: 2026-08-12
> 🏷️ 版本代号: **"Visualization Plus"** — 让数据可视化更交互、更直观
> 🔖 上一版本: v1.0.1.4

---

## 🎯 本次更新一句话总结

**核心改进**：全新添加"时间维度分析"Tab页，支持按日、周、月、季度、年度多粒度统计分析，配备同比/环比增长率图表，实现全方位安全态势洞察。

---

## 📊 可视化图表增强

### 1. 风险分布饼图增强

**文件**: [StatisticsDashboardWindow.xaml.cs](file:///D:/360安全浏览器下载/软件开发备份/网络安全漏洞扫描/NetSecurityScanner/src/NetSecurityScanner.Desktop/Views/Views/StatisticsDashboardWindow.xaml.cs)

**改进**:
| 属性 | 改进前 | 改进后 |
|------|--------|--------|
| 工具提示 | 无 | 显示漏洞数量和百分比 |
| 数据标签 | 基础显示 | 仅显示 >5% 的区块，带百分比 |
| 初始角度 | 默认 | 从 -90° 开始（12点钟位置） |
| 提示样式 | 默认 | 白底黑字，中文字体 |

### 2. 漏洞趋势柱状图增强

**改进**:
| 属性 | 改进前 | 改进后 |
|------|--------|--------|
| 数据标签 | 无 | 柱顶显示数值 |
| 工具提示 | 无 | 悬停显示"严重: X个"或"高危: X个" |
| 网格线 | 无 | 添加浅灰色网格线 |
| 图例 | 无 | 底部显示可点击图例 |

### 3. 扫描趋势折线图 - 核心改进

**改进**:
| 属性 | 改进前 | 改进后 |
|------|--------|--------|
| 指标数量 | 单一（仅扫描次数） | 三指标（扫描次数+漏洞数+开放端口） |
| 填充效果 | 无 | 半透明填充区域 |
| 数据标签 | 无 | 非零值点显示标签 |
| 工具提示 | 无 | 悬停显示具体数值和单位 |
| 图形标记 | 简单圆点 | 带白边彩色圆点 |
| 网格线 | 无 | 添加浅灰色网格线 |
| 图例 | 无 | 底部显示可点击图例 |

### 4. 漏洞类型分布图增强

**改进**:
| 属性 | 改进前 | 改进后 |
|------|--------|--------|
| 填充色 | 无 | 紫色填充 |
| 边框 | 细线 | 白色粗边框 |
| 数据标签 | 无 | 柱顶显示数值 |
| 工具提示 | 无 | 显示漏洞名称和出现次数 |
| 网格线 | 无 | 添加浅灰色网格线 |

### 5. 端口分析图增强

**改进**:
| 属性 | 改进前 | 改进后 |
|------|--------|--------|
| 填充色 | 蓝色 | 蓝色 + 白色边框 |
| 数据标签 | 无 | 柱顶显示数值 |
| 工具提示 | 无 | 显示端口号和出现次数 |
| 网格线 | 无 | 添加浅灰色网格线 |

---

## 🆕 新增功能：时间维度分析 Tab

### 1. 时间粒度选择器

新增时间粒度下拉框，支持以下维度：
- **按日**：最近30天按日统计
- **按周**：最近12周按周统计
- **按月**：最近12月按月统计
- **按季度**：最近8季度按季度统计
- **按年**：最近5年按年统计

### 2. 对比期间选择器

支持多种对比模式：
- 与上一周期（默认）
- 与上一月同期
- 与上一季度同期
- 与去年同月
- 无对比

### 3. 多周期统计卡片

新增5个时间维度统计卡片，每个卡片显示：
- 📅 **今日**：今日扫描次数 + 漏洞数 + 环比增长率
- 📆 **本周**：本周扫描次数 + 漏洞数 + 环比增长率
- 📅 **本月**：本月扫描次数 + 漏洞数 + 环比增长率
- 📊 **本季度**：本季度扫描次数 + 漏洞数 + 环比增长率
- 🗓️ **本年度**：本年度扫描次数 + 漏洞数 + 同比增长率

### 4. 时间维度趋势图

折线图同时展示三个指标：
- **扫描次数**（蓝色）
- **漏洞数**（红色）
- **开放端口**（绿色）

支持切换时间粒度，动态更新图表数据。

### 5. 同比/环比增长率图

柱状图展示6个周期的同比/环比增长率：
- **扫描增长**（蓝色柱）
- **漏洞增长**（红色柱）
- **端口增长**（绿色柱）

正值（绿色）表示增长，负值（自动显示为相对颜色）表示下降。

---

## 🛠️ 技术改进

### 数据模型增强

1. **PeriodStatisticsResult 类**
   - 新增 `CurrentPortCount` 属性

2. **新增数据模型类**
   - `TimeDimensionItem`：时间维度统计项
   - `ComparisonItem`：对比数据项

### 方法增强

1. **CalculatePeriodStats**
   - 新增 `CurrentPortCount` 计算

2. **新增方法**
   - `TimeGranularityComboBox_SelectionChanged`：粒度选择事件
   - `ComparePeriodComboBox_SelectionChanged`：对比期间选择事件
   - `RenderTimeDimensionAnalysis`：渲染时间维度分析
   - `RenderTimeDimensionCards`：渲染时间维度统计卡片
   - `RenderTimeDimensionTrendChart`：渲染时间趋势图
   - `RenderComparisonChart`：渲染同比/环比对比图
   - `FormatGrowth`：格式化增长率显示
   - `CalculateGrowthRate`：计算增长率

---

## 🎨 统一视觉改进

所有图表统一应用以下改进：
- **中文字体**: 全部使用 "Microsoft YaHei"
- **提示框**: 白底黑字，统一样式
- **网格线**: 浅灰色 `#E8E8E8`
- **数据标签**: 统一大小
- **坐标轴标签**: 深灰色 `#2C3E50`
- **图例位置**: 统一底部显示

---

## 🔧 技术细节

- **图表库**: LiveChartsCore.SkiaSharpView.WPF 2.0.0-rc2
- **.NET 版本**: net6.0-windows
- **WPF 版本**: Windows Desktop SDK

---

## 📋 验证清单

- [x] 风险分布饼图工具提示正常工作
- [x] 漏洞趋势柱状图显示数据标签
- [x] 扫描趋势图显示三个指标
- [x] 漏洞类型图工具提示显示完整名称
- [x] 端口分析图工具提示显示端口号
- [x] 所有图表图例可点击
- [x] 中文显示正常
- [x] 时间维度 Tab 页正常加载
- [x] 时间粒度切换正常响应
- [x] 对比期间切换正常响应
- [x] 今日/本周/本月/本季度/本年度统计卡片正常显示
- [x] 环比/同比增长率计算正确
- [x] 时间维度趋势图正常渲染
- [x] 同比/环比对比图正常渲染
- [x] 版本号更新至 1.0.1.5

---

## 🐛 问题修复

- **PeriodStatisticsResult 类** 新增 `CurrentPortCount` 属性，确保端口数量统计完整

---

## 🚀 性能优化

- 图表渲染使用硬件加速（SkiaSharp）
- 数据点仅在非零时显示标签，减少视觉噪音
- 异步加载统计数据，避免 UI 阻塞
- 使用 try-catch 隔离各模块错误，确保单模块失败不影响整体

---

*此版本为可视化增强版本，核心扫描功能保持不变。*
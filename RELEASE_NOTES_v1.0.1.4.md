# NetSecurityScanner v1.0.1.2 → v1.0.1.4 累计更新介绍

> 📅 更新跨度: 2026-08-07(同日完成 1.0.1.3 / 1.0.1.4 两个迭代)
> 🎯 更新主题: **从"能用"到"可靠"——资产管理加固 + 综合扫描重做 + 端口可见性修复**
> 📦 累计新增代码: ~2,800 行
> 🐛 累计修复 Bug: 7 个(含 2 个崩溃级 + 1 个数据丢失级)

---

## 🧭 一图看懂版本演进

```
v1.0.1.2 ──资产管理(可用了)
   │
   │  v1.0.1.3  ◀── 重做综合扫描(可看全貌了)
   │     · 6 阶段编排 + 5 Tab 展示 + 4 操作
   │     · 实时进度窗口 + 3 种导出格式
   │
   │  v1.0.1.4  ◀── 修底层 + 修显示(可信了)
   │     · 端口风险表崩溃修复
   │     · 漏洞库 3/31 → 31/31
   │     · UDP "开放或过滤"端口可见
   │     · 资产文件损坏保护
   │
   ▼
v1.0.1.4  ◀── 你现在用的版本
```

---

## 📊 三个版本能力对照表

| 能力维度 | v1.0.1.2 | v1.0.1.3 | v1.0.1.4 |
|----------|:--------:|:--------:|:--------:|
| **资产管理窗口** | ✅ 新增 | ✅ 保持 | ✅ 损坏保护 |
| **资产 RBAC 权限** | ✅ 新增 | ✅ 保持 | ✅ 保持 |
| **综合扫描编排** | ❌ 散落在 MainWindow | ✅ 独立 Service | ✅ 保持 |
| **扫描阶段可视化** | ❌ 单一进度条 | ✅ 6 阶段时间线 | ✅ 保持 |
| **结果窗口 Tab** | 1 个(混合) | 5 个(分类) | ✅ 5 个 |
| **TCP 端口可见** | ✅ | ✅ | ✅ |
| **UDP "开放或过滤"** | ❌ 不显示 | ❌ 不显示 | ✅ 可见 |
| **漏洞库加载数** | 3 / 31 | 3 / 31 | ✅ 31 / 31 |
| **结果导出格式** | 仅 JSON | JSON + CSV + MD | ✅ 保持 |
| **历史对比** | ❌ | ✅ 增量对比 | ✅ 两种状态都对比 |
| **崩溃保护** | ❌ | ❌ | ✅ 4 处防御 |
| **数据丢失保护** | ❌ | ❌ | ✅ 资产损坏自动备份 |

---

## 🆕 v1.0.1.3 — 综合扫描重做(从 230 行到 35 行)

**核心改动**:把原本散落在 `MainWindow.xaml.cs` 里 230+ 行的 `StartScan_Click` 拆解为独立服务 + 独立窗口 + 独立结果视图。

### 1. 新增 `ComprehensiveScanService` —— 6 阶段编排器

按顺序执行,任一阶段失败不影响后续:

```
① 主机存活检测 → ② TCP/UDP 并发扫描 → ③ 服务识别
   → ④ 漏洞匹配 → ⑤ 插件扫描 → ⑥ 风险聚合
```

每阶段都有状态、耗时、错误信息,失败不中断。

### 2. 新增 `ComprehensiveScanResultWindow` —— 5 Tab 分类展示

| Tab | 内容 |
|-----|------|
| **概览** | 目标/耗时/阶段时间线/风险徽章 |
| **端口扫描** | 端口号/协议/状态/服务 |
| **漏洞** | CVE 列表,带严重程度筛选器 |
| **服务识别** | 按服务名聚合,显示端口和版本 |
| **插件扫描** | 业务插件匹配结果 |

### 3. 新增 `ScanProgressLiveWindow` —— 实时进度窗口

不再"黑盒等待",每完成一个端口就立刻在进度窗口里看到,阶段切换有提示音 + 状态栏。

### 4. 新增 `ScanExportService` —— 3 种导出格式

- **JSON**:结构化,适合二次处理
- **CSV**:Excel 直接打开
- **Markdown 复制**:一键贴到周报/工单

### 5. 4 个结果操作

导出 JSON、导出 CSV、对比历史、复制 Markdown,4 个按钮直接放在结果窗口顶部。

---

## 🔧 v1.0.1.4 — 修底层 + 修显示(7 个 Bug)

### 🛑 P0:综合扫描结果窗口"看不到端口"

**用户反馈**: "还是扫描不到端口"

**根因**: `LoadPorts()` 只过滤 `Status == "开放"`,而 UDP 扫描因协议无连接特性,常返回"**开放或过滤**"。结果窗口把这些端口直接过滤掉,显示为空。

**修复**: 3 处过滤逻辑全部放宽到两种状态,详见 [CHANGELOG_v1.0.1.4.md](file:///D:/360安全浏览器下载/软件开发备份/网络安全漏洞扫描/CHANGELOG_v1.0.1.4.md)

**效果**: 综合扫描结果窗口的"端口扫描""服务识别""历史对比"三个 Tab 全部恢复正常。

---

### 💥 P1-1: 端口风险表崩溃(`TypeInitializationException`)

**触发条件**: 首次启动时静态构造 `PortRiskScorer`,1433/8080/9090 三个键重复 → 抛 `ArgumentException` → 整个扫描功能无法启动。

**修复**: 统一从权威源获取风险评级,删除冲突条目。

---

### 💥 P1-2: 服务映射字典冲突

**触发条件**: `PortServiceMapping` 中 80/8080/8443/9000 多条记录 → `Dictionary` 初始化失败。

**修复**: 合并服务映射,移除冗余条目。

---

### ⚠️ P1-3: 漏洞数据库只解析 3/31 条

**根因**: `JsonSerializer.Deserialize` 默认大小写敏感,JSON 中 `CVE_ID` 字段解析失败。

**修复**: 添加 `PropertyNameCaseInsensitive = true`。

**影响**: 2023 年最新 CVE 全部可用(CVE-2023-44487 HTTP/2 拒绝服务、CVE-2023-22515 Confluence 鉴权绕过等)。

---

### ⚠️ P1-4: UDP 扫描显示关闭端口

**现象**: UDP 扫描结果里出现一长串"关闭"状态端口,干扰判断。

**修复**: 在 `ScanUdpPortsAsync` 末尾添加与 TCP 对齐的过滤,只返回"开放"或"开放或过滤"。

---

### 🛡️ P1-5: 资产管理数据丢失风险

**隐患**: `assets.json` 损坏时,`JsonSerializer.Deserialize` 抛异常,`_assets` 保持空。**用户在 UI 上看到"无资产",如果此时点保存,会直接覆盖原文件,数据永久丢失。**

**三重保护**:

1. **自动备份**:`assets.json` → `assets.json.corrupt-20260807_181122_345.bak`
2. **拒绝覆盖**: `_loadFailed` 标志为 true 时,`SaveAssets()` 直接返回,不写盘
3. **用户反馈**: `LastLoadError` 属性把错误信息和备份位置显示在 UI 上

---

### 🛡️ P1-6: 新增 2 个测试工具

- **`Tools/PortVerify/Program.cs`**: 5 秒验证端口扫描底层是否工作
- **`Tools/DeepTest/Program.cs`**: 6 个端到端回归场景,扫描 1000 端口 10.3s,匹配 15 条漏洞

---

## 🎁 累计新增文件清单

### 全新模块(8 个)

| # | 文件路径 | 模块职责 |
|---|----------|----------|
| 1 | `NetSecurityScanner.Core/Services/ComprehensiveScanService.cs` | 6 阶段编排 |
| 2 | `NetSecurityScanner.Core/Services/ScanExportService.cs` | JSON/CSV 导出 |
| 3 | `NetSecurityScanner.Desktop/Views/ComprehensiveScanResultWindow.xaml(.cs)` | 5 Tab 结果窗口 |
| 4 | `NetSecurityScanner.Desktop/Views/ScanProgressLiveWindow.xaml(.cs)` | 实时进度窗口 |
| 5 | `NetSecurityScanner.Desktop/Views/LoginWindow.xaml(.cs)` | 登录界面 |
| 6 | `NetSecurityScanner.Desktop/Views/AssetManagementWindow.xaml(.cs)` | 资产管理 |
| 7 | `Tools/PortVerify/Program.cs` | 端口验证器 |
| 8 | `Tools/DeepTest/Program.cs` | 深度回归测试 |

### 修改文件(累计 10+ 个)

- `MainWindow.xaml.cs`:综合扫描入口从 230 行精简到 35 行
- `AssetManagementService.cs`:损坏保护
- `PortRiskScorer.cs`:去重
- `PortServiceMapping.cs`:去重
- `VulnerabilityScanner.cs`:JSON 大小写不敏感
- `PortScanner.cs`:UDP 过滤

---

## 💡 升级带来的核心价值

### 1. 用户体验层面

| 之前 | 现在 |
|------|------|
| 扫描进度看不到,只能干等 | 实时显示每个阶段 + 每个端口 |
| 结果列表全混在一起,找不到想要的 | 5 个分类 Tab,想看什么点什么 |
| 报告要手写 | 一键导出 JSON / CSV / Markdown |
| UDP 端口"扫不到"(其实是显示问题) | UDP 开放或过滤端口正常显示 |
| 扫完不知道风险多高 | 风险徽章 + 阶段时间线 + 风险聚合 |

### 2. 稳定性层面

| 隐患 | 现在的保护 |
|------|-----------|
| 端口风险表键重复 → 启动崩溃 | 已修复,静态初始化稳定 |
| 服务映射冲突 → 部分端口无法识别 | 已修复,所有端口可识别 |
| 资产文件损坏 → 静默清空 | 三重保护(备份 + 拒绝覆盖 + 错误反馈) |
| 漏洞库解析不全 → 漏报 28 条 CVE | 31/31 完整加载 |
| UDP 结果噪音 → 干扰判断 | 过滤后只显示有效状态 |

### 3. 工程层面

- `StartScan_Click` 从 230 行精简到 35 行(-85%)
- 综合扫描逻辑从 UI 层抽到 Service 层,可单元测试
- 2 个独立测试工具,回归验证 5 秒完成
- 所有修复符合 Trae 双模式编程规范(Work 模式定义 + Code 模式实现 + 审查 + 冒烟)

---

## 📋 测试验证摘要

| 场景 | 端口扫描 | 漏洞匹配 | 状态 |
|------|----------|----------|------|
| 127.0.0.1 + Standard | 14 端口(4 开放 + 10 开放或过滤) | 15 条匹配 | ✅ 通过 |
| 1-1000 端口发现 | 10.3 秒完成 | - | ✅ 通过 |
| 漏洞匹配 2023 CVE | - | 8/8 命中(含 CVE-2023-44487) | ✅ 通过 |
| 不可达目标 | 0 端口 | 0 漏洞 | ✅ 通过 |
| 资产损坏场景 | - | - | ✅ 备份成功,数据零丢失 |

---

## ⚠️ 使用提示

1. **关闭旧版**:升级前先关闭正在运行的 `NetSecurityScanner.Desktop.exe`
2. **数据兼容**:资产数据格式未变,无需迁移
3. **新功能入口**:
   - 登录界面:首次打开资产管理时自动弹出
   - 综合扫描:顶部菜单 → 工具 → 综合扫描
   - 实时进度:综合扫描时自动弹出
4. **历史对比**:同一目标需扫描 2 次以上,才能在结果窗口使用"历史对比"按钮

---

## 🔮 下次迭代方向

- [ ] `PortScanResult` 模型添加 `Protocol` 字段(TCP/UDP 区分)
- [ ] `ComprehensiveScanService` xUnit 单元测试
- [ ] PDF 报告导出
- [ ] 主窗口"端口扫描"Tab 与综合扫描结果联动
- [ ] 漏洞库远程更新机制(目前是本地 JSON)

---

> 📝 **附**:本次更新的具体技术细节(行号、代码片段、修复前后对比)请见
> [CHANGELOG_v1.0.1.4.md](file:///D:/360安全浏览器下载/软件开发备份/网络安全漏洞扫描/CHANGELOG_v1.0.1.4.md)

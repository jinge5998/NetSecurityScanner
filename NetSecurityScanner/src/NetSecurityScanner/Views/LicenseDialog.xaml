<Window x:Class="NetSecurityScanner.Views.LicenseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="授权码管理" Width="520" Height="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="📋 当前授权状态" Margin="0,0,0,15" FontSize="13">
            <StackPanel>
                <TextBlock Name="StatusTextBlock" Text="未授权" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Name="LicenseTypeTextBlock" Text="授权类型：无" Margin="0,0,0,4" FontSize="12"/>
                <TextBlock Name="ExpiryTextBlock" Text="到期时间：-" Margin="0,0,0,8" FontSize="12"/>

                <Border Background="#ECF0F1" CornerRadius="4" Padding="10" Margin="0,5,0,0">
                    <StackPanel>
                        <TextBlock Text="机器码（复制后发给管理员获取授权码）" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Name="MachineIdTextBox" IsReadOnly="True" FontSize="11"
                                     Foreground="#2C3E50" Background="Transparent" BorderThickness="0"
                                     TextWrapping="Wrap" FontWeight="SemiBold"/>
                            <Button Grid.Column="1" Name="CopyMachineIdButton" Content="📋 复制机器码"
                                    FontSize="12" Padding="12,6" Margin="8,0,0,0"
                                    Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"
                                    Click="CopyMachineIdButton_Click" FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="🔑 激活授权码" Margin="0,0,0,15" FontSize="13">
            <StackPanel>
                <TextBox Name="LicenseCodeTextBox" Height="40" FontSize="14" FontWeight="SemiBold"
                         VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Name="ActivateButton" Content="✅ 激活" Width="120" Height="36" Margin="0,0,10,0"
                    Background="#27AE60" Foreground="White" FontWeight="Bold" FontSize="14"
                    Click="ActivateButton_Click"/>
            <Button Name="CloseButton" Content="关闭" Width="100" Height="36" FontSize="13"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

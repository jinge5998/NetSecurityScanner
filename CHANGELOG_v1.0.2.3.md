# 更新日志 v1.0.2.3

发布日期：2026-09-25

## 概述

本版本以授权码生成器独立化为核心：工具从主程序中分离为独立项目与独立发行包，完善机器码绑定、发行记录管理等能力；主程序功能与 v1.0.2.2 保持一致，仅版本号升级。

## 主要变更

### 授权码生成器独立化

- 生成器从主程序菜单分离，独立为 `tools/LicenseKeyGenerator` 项目，并建立根目录独立发行文件夹 `LicenseKeyGenerator/`，便于单独升级与分发
- 发行版：`LicenseKeyGenerator-v1.0.2.3.zip`（自包含，目标电脑无需安装 .NET 运行时）

### 授权码生成器功能完善

- **完整机器码绑定**：支持直接粘贴目标机器完整机器码（32 位十六进制），自动计算绑定前缀（SHA256 前 8 字节），与主程序 `LicenseService` 校验算法完全一致
- **发行时间修复**：默认发行时间改为动态取当前时间，新增「🕒 现在」一键刷新按钮；修复 v1.0.2.2 中默认时间 12:00:00 在 11:55 之后触发"发行时间不能超过当前时间+5分钟"导致无法生成的问题
- **发行记录表格**（9 列）：序号、机器码、授权码、类型（5分钟/1年期/2年期/永久 中文显示）、发行时间、到期时间、发行者、备注、操作（复制/删除）
  - 记录按发行时间倒序，序号连续编号，删除后自动重排
  - 机器码/授权码/备注列悬停显示全文
- **CSV 导出**：同步为相同 8 列结构（序号、机器码、授权码、类型、发行时间、到期时间、发行者、备注）

### 主程序

- 版本号升级至 1.0.2.3（Core / Desktop / LicenseKeyGenerator / 旧 WPF / Linux 全系列 csproj）
- 升级渠道保持：GitHub 主升级优先，百度网盘辅助

## 质量验证

- 单元测试：248/248 通过（Release）
- 授权算法端到端测试：7/7 通过
- 发行版 EXE 版本验证：1.0.2.3

## 升级方式

1. **GitHub（推荐）**：程序内「检查更新」自动下载 `NetSecurityScanner-Windows-v1.0.2.3.zip` 一键升级
2. **百度网盘**：下载 `NetSecurityScanner-Windows-v1.0.2.3.zip` 解压后覆盖安装目录
3. **升级包目录**：`update-v1.0.2.3/` 与 zip 内容相同，便于手动拷贝

## 交付物清单

| 文件/目录 | 说明 |
|---|---|
| `NetSecurityScanner-Windows-v1.0.2.3.zip`（81.9MB） | 主程序发行版（自包含） |
| `update-v1.0.2.3/` | 升级包目录（与 zip 内容一致） |
| `LicenseKeyGenerator-v1.0.2.3.zip`（66.1MB） | 授权码生成器发行版（自包含） |
| `LicenseKeyGenerator/` | 授权码生成器独立发行文件夹 |
| `NetSecurityScanner/publish-v1.0.2.3/` | 主程序发布产物源目录 |

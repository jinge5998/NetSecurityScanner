# 🔑 授权码生成器 v1.0.2.3 - 发布说明

发布日期：2026-09-25

## 概述

授权码生成器是网络安全扫描工具的配套管理工具，用于为授权用户机器生成离线授权码。自 v1.0.2.3 起从主程序中独立发行，便于单独升级与分发。

- 发行包：`LicenseKeyGenerator-v1.0.2.3.zip`（66.1MB，自包含，**目标电脑无需安装 .NET 运行时**）
- 运行方式：解压后直接运行 `LicenseKeyGenerator.exe`

## 功能特性

### 1. 完整机器码绑定

- 直接粘贴目标机器的**完整机器码（32 位十六进制）**，工具自动计算绑定前缀（SHA256 前 8 字节）
- 绑定算法与主程序 `LicenseService` 校验完全一致，生成的授权码只能在目标机器激活
- 输入实时校验：非十六进制字符、长度不足（提示"还需 N 个字符"）均有颜色提示
- 「📡 本机」按钮一键填入本机机器码

### 2. 四种授权类型

| 类型 | 类型码 | 有效期 |
|---|---|---|
| 试用授权 | T | 5 分钟 |
| 1 年期授权 | 1 | 发行时间 + 1 年 |
| 2 年期授权 | 2 | 发行时间 + 2 年 |
| 永久授权 | P | 永不过期 |

### 3. 发行时间管理

- 默认发行时间自动取当前时间，避免超出"当前时间 + 5 分钟"校验限制
- 「🕒 现在」按钮一键刷新发行时间
- 发行时间精确到秒，写入授权码时间戳

### 4. 发行记录（9 列表格）

| 列 | 说明 |
|---|---|
| 序号 | 连续编号，最新记录在前，删除后自动重排 |
| 机器码 | 完整 32 位机器码（悬停显示全文） |
| 授权码 | 完整授权码（悬停显示全文） |
| 类型 | 5分钟 / 1年期 / 2年期 / 永久（中文显示） |
| 发行时间 | yyyy-MM-dd HH:mm |
| 到期时间 | 永久类型显示"永久" |
| 发行者 | 操作人 |
| 备注 | 超长省略号显示，悬停看全文 |
| 操作 | 📋 复制授权码 / 🗑️ 删除记录 |

- 记录持久化存储于生成器同目录，重启不丢失
- 支持 **CSV 导出**（序号、机器码、授权码、类型、发行时间、到期时间、发行者、备注）

### 5. 安全机制

- 授权码采用 **HMAC-SHA256** 签名，格式：`绑定前缀-发行时间戳-类型码-签名`
- 生成前自动本地校验：格式、时间戳、类型码、HMAC、机器码绑定、有效期全链路验证
- 与主程序激活校验算法一致性已通过端到端测试（7/7）验证

## 使用流程

1. **获取机器码**：在目标电脑上运行主程序，进入「授权管理 → 授权码激活」界面查看本机完整机器码（32 位）
2. **生成授权码**：在生成器中粘贴完整机器码 → 确认"绑定前缀"自动显示 → 选择授权类型 → 确认发行时间 → 点击「🚀 生成授权码」
3. **验证与交付**：右侧校验信息全绿（机器码匹配 / HMAC / 到期时间）后，点击「📋 复制」将授权码发给用户
4. **激活**：用户在主程序「授权管理 → 授权码激活」中粘贴授权码完成激活
5. **记录归档**：点击「💾 保存并记录」留存发行记录，可随时 CSV 导出

## 系统要求

- Windows 10 / 11（x64）
- 无需 .NET 运行时（自包含发布）
- 建议以管理员身份运行（发行记录写入同目录）

## 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| 1.0.2.3 | 2026-09-25 | 独立发行；完整机器码绑定；发行时间默认当前时间并新增「现在」按钮（修复默认 12:00 超限无法生成）；发行记录 9 列表格与 CSV 导出；类型中文显示 |

## 安全提示

- 生成器内置 HMAC 密钥与主程序共享，**请妥善保管本工具，仅限授权管理员使用**
- 任何持有本工具的人都可以为任意机器码生成有效授权码，切勿外传
- 发行记录包含全部授权历史，建议定期导出 CSV 归档
